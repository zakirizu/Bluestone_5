package selenium;

public class Xpath {

	public static void main(String[] args) {
	
		
		// Write a function that returns true if the brackets in a given string are balanced.
		// Balanced means that every parenthesis/bracket or brace that is opened must be closed, and it must be closed in the right order (always close the last symbol you opened).
		// The function must handle parens (), square brackets [], and curly braces {}.

		// Test expressions:
		// '(a[0]+b[2c[6]]) {24+53})'
		// 'f(e(d))[()]{}([])' -> valid
		// '((b)'
		// '(c]'
		// '{(a[])'
		// '([)]'
		// ')('
		// '' -> valid
		
		
		// Tell him I have logic in mind but ,,.. may take time to write 
		//tell him I can sort the string and then check if I have replica .. by that I mean even and odd counts correctly for every things in the given string
		
		 
		
		
		
		
		
		
		
		
		
		/*
		
		tablex
		Z	A	K	I	R
		B	E	Y	O	U
		
		
		
		
		
xpath for letter U
		//tablex/tr[2]/td[5]
		
xpath for letter Z
		//tablex/tr[1]/td[1]
tr 	= row
tr 	= column
[1] = predicate- that holds intiger value by defaults it takes value as 1	
		
*/
	}

}
